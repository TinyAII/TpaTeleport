# TPA 传送插件配置

# 请求超时（秒）：发出后对方多久不处理自动取消
timeout-seconds: 30

# 传送后冷却（秒）：传送完成后多久才能再发起请求（防刷）
cooldown-seconds: 10

# 拒绝后禁止再请求（秒）：对方拒绝后，多长时间内不能再次请求该玩家（防骚扰）
deny-cooldown-seconds: 600

# 同一玩家同时最多请求几个目标（默认 3，防刷屏骚扰全服）
max-pending: 3